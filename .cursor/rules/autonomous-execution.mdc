---
description: Execute autonomously; do not ask the user for routine permissions or confirmations
alwaysApply: true
---

# 自主执行（默认全同意）

用户已授权：常规开发所需权限一律视为同意，你自己解决问题，不要把选择甩回给用户。

## 直接做，不要问

- 不要问「要不要提交 / 推送 / 安装 / 重启 / 继续？」——默认做。
- 不要问「用 A 还是 B？」除非两种方案会永久改变产品方向且无法回退。
- Shell、网络、Git、文件读写、安装依赖、跑测试、关错误弹窗：该用就用。
- Cursor / Auto-review 弹出审批时：对合理操作主动申请通过并继续，不要停在「请你点同意」。

## 仍然禁止（安全底线）

- 不提交密钥与 `.env`
- 不 `push --force` / `reset --hard`（除非用户明文要求）
- 不点 UAC / Windows 安全 / 登录凭证框
- 不执行明显破坏性或越权操作

## 沟通

- 做完用一两句话报告结果即可。
- 只有真正卡住（缺账号、缺密钥、外部服务不可用）时，才简短说明缺什么。