---
description: Auto-git once per user turn over SSH; business commit titles; safe sync
alwaysApply: true
---

# Git：一轮需求一次提交

本仓库自动 Git。用户每发一条需求，改完后**只产生一个 commit**，再 pull / push。

## 必须遵守

1. **不要自己 `git commit` / `git push` / `git reset`**（Shell 护栏会拦）。整轮改完只写 `.cursor/hooks/pending-commit-msg.txt`，由 `stop` 钩子一次性 `add + commit + pull --no-rebase + push`。
2. **标题从业务写**：中文、让用户看懂「做了什么」。第一行是标题，空一行后可写正文。
   - 好：`为恋综模拟器加入每日签到与连续奖励`
   - 差：`auto: foo.ts`、`update`、`fix`、文件名列表
3. **远程只用 SSH**：`git@github.com:...`。SSH 可执行文件：`C:/Program Files/Git/usr/bin/ssh.exe`。
4. **禁止** `--force` / `reset --hard` / rebase 已推送历史（除非用户明文要求）。
5. **不提交机密**：`.env`、密钥、token、`credentials.json`、证书。
6. **空提交不做**。若 `auto-git-status.json` 显示 pull 冲突：停下说明冲突文件，不要强推。

## 钩子分工

- **`stop`**：`auto-git.ps1` 是唯一提交者，本轮所有改动进同一个 commit。
- **`beforeShellExecution`**：拦截中途 `git commit`/`push`/`reset`/`rebase`。
- **`afterFileEdit`**：不跑 auto-git。
