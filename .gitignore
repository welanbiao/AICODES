# Secrets — never commit
.env
.env.*
!.env.example
credentials.json
*.pem
*.key
*.p12
*.pfx
*.keystore
*.secret
id_rsa
id_dsa
id_ed25519
local.properties

# OS
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# Editor swap files
*.swp
*.swo
*~

# Accidental Git helper dumps
.git-err.txt
.git-out.txt

# Nested standalone repo — tracked separately at git@github.com:welanbiao/zongcai-lu-yanci.git
# Adding this directory would create a gitlink/submodule pointer, not the project files.
/总裁/
